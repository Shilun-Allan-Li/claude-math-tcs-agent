# Phase 0 findings (Claude Code 2.1.269, 2026-09-13)

Verified on this machine with headless sessions (`claude -p --plugin-dir …`) and direct toolchain calls.

| # | Question | Answer | Consequence |
|---|---|---|---|
| a | How are plugin agents addressed in the Agent tool / workflow `agentType`? | **`math-tcs:tool-runner`** (plugin-prefixed). Bare `tool-runner` fails: `agent type 'tool-runner' not found. Available agents: claude, Explore, general-purpose, math-tcs:tool-runner, Plan, statusline-setup`. Skills are `math-tcs:<name>`. | `run.js` uses `args.agentPrefix = "math-tcs:"`; stage skills say "the `math-tcs:semantic-reviewer` agent". |
| b | Can a skill call the Workflow tool with `scriptPath` under `${CLAUDE_PLUGIN_ROOT}`? | The Workflow tool works headlessly and returns `runId` + transcript dir, but **refuses a `scriptPath` outside the session's working directory** (the plugin dir is outside the target project). Passing the script inline works. | `mathtcs.py workflow stage` copies `workflows/run.js` into `<target>/math-tcs/workflows/run.js`; the `run` skill passes that path (stable for `resumeFromRunId`). |
| c | Do the agent file's `tools`/`model` apply under `agentType`? | The haiku `tool-runner` ran its Bash command and returned schema-validated JSON (cost of the whole probe run: $0.31). Tool restriction is by the agent definition. | Reviewers get no Write/Edit/Bash in their frontmatter; that is the read-only guarantee. |
| d | `!` injection with `$ARGUMENTS`; `${CLAUDE_PLUGIN_ROOT}` expansion | `/math-tcs:ping hello world --until verify` injected `{"stage": "ping", "tokens": ["hello","world","--until","verify"]}` and the absolute plugin root. `allowed-tools: Bash(python3 *mathtcs.py*)` pre-approved the injected command. | Argument parsing is done by `mathtcs.py args <stage> $ARGUMENTS`. |
| e | `lake lean <file> -- --json` on v4.25 (Lake 5.0.0) | Works; one JSON object per line: `severity` ∈ `error|warning|information`, `kind: "hasSorry"` for sorry warnings, `pos`/`endPos` 1-based lines, `#print axioms` as an `information` message `'X' depends on axioms: [...]`; ~20 s cold for `import Mathlib.Data.Nat.Basic`. Transitive-sorry case reports `[sorryAx]` with no textual sorry. | `lean_check.py` runs `lake lean … -- --json`, fallback `lake env lean --json`. |
| f | `lakefile.lean` lib detection (tcslib) | `lean_lib «TCSlib» { … }` with no `srcDir` → `src_dir = "."`, `module_prefix_guess = "TCSlib"` because `TCSlib.lean` / `TCSlib/` exist; `TCSlib.lean` is a hand-maintained import root (new modules are not built by `lake build` until registered). | Config keeps `lib`, `src_dir`, `module_prefix`, `root_file`, `register_in_root` (default false; checking uses per-file `lake lean`, which needs no registration). |
| g | `claude plugin validate --strict` | Passes for `plugins/math-tcs` and for the repo-root marketplace. | CI/Makefile target. |
